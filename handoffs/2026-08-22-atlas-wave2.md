# Atlas wave 2 handoff — 2026-08-23

**Status:** built, tested, reviewed, live-smoked on `claude/atlas-revamp` (worktree
`C:\Users\danie\Atlas-worktrees\revamp`). Supersedes the PM2 cutover step in
`2026-08-22-atlas-revamp.md`: Atlas is now an app you open.
**Spec:** `docs/specs/2026-08-22-atlas-wave2-design.md` · **Plan:** `docs/plans/2026-08-22-atlas-wave2-plan.md`

## What changed

1. **Listening discipline (July gate reinstated).** `worker/addressing.py` + the silence clock in
   `worker/engagement.py`. After wake, speech is handled if it arrives within 30 s of Atlas's last
   activity, or contains "atlas", or hits the vocabulary (every app word from `config/apps.yaml` +
   `address_vocab` in `atlas.yaml`). Everything else lands in the transcript as `ambient` with no model
   call and no TTS. Two minutes without directed interaction → **silent** auto-sleep (mic muted, state
   ASLEEP, transcript `auto-sleep`, nothing spoken). "Go to sleep" / "that's all" still get the sleep line.
2. **Desk reach.** `find_file`, `open_file`, `read_file` over configured `file_roots`
   (`~/Desktop, ~/Documents, ~/Downloads, C:/Users/danie/kb`), and `close` (graceful `taskkill /IM`
   without `/F`, signed profiles only). Quick questions about a file are answered in-turn; real analysis
   goes to `launch_work`.
3. **Correct counts.** `count_mail` pages the Gmail MCP search (500/page, ≤ 4 pages) and returns an
   exact count. Live: "how many unread emails" → **2**; "how many emails total" → **60** (matches the raw
   MCP probe).
4. **The Atlas app.** `python -m worker.desktop` (or the Start-menu shortcut from
   `scripts/install_shortcut.ps1`) opens a native WebView2 window on the paired command center and runs
   the voice worker as its child. Close the window → Atlas is off (child tree terminated, port released).
   PM2 files are gone.

## Live evidence on this machine

- `count_mail`: 2 unread / 60 total, ~2 s per turn after MCP connect.
- `find_file`: full kb scan in 0.43 s with correct hits; out-of-root files correctly not found.
- App: open → worker child + `/state` up in 12.9 s (ASLEEP, mic armed); a second `worker.desktop` is
  refused by the instance mutex (no second worker spawned); close window → authenticated `/shutdown`,
  launcher exits in 3.1 s, 0 `worker.app` processes, port 4360 free.
- After the review fixes: "find the atlas voice layer design spec and open it" → token-matched
  `find_file` hit on `kb\docs\specs\2026-07-15-atlas-voice-layer-design.md` (first attempt had missed it
  on raw substring matching — fixed), then `open_file`.
- Process hygiene check: the ~20 `workspace-mcp` processes on the machine all belong to live Claude Code
  terminals (`claude.exe → uvx → uv → workspace-mcp → python`, five per session) — none are Atlas
  leftovers. Atlas now additionally puts itself and its children in a kill-on-close Job Object and
  tree-kills MCP servers on close, so it cannot leak them either.

## Review

- Sonnet independent verifier: PASS (suite, dead references, secret scan, spec conformance, docs vs code);
  flagged `open_file` executing `.exe/.lnk` — fixed below.
- Codex `gpt-5.6-sol` adversarial review (read-only, xhigh): 19 findings; 17 fixed by two parallel
  Codex workers: curated address vocabulary (only `gmail, inbox, unread, calendar, youtube, notion, github,
  spotify, workers` count as address outside the 30 s window — generic verbs are gone), reflexes respect the
  gate (`repeat`/`cancel` need an addressed, engaged turn; `dismiss` stays global), auto-sleep never cuts a
  turn in flight, `open_file` opens inert documents only and refuses reparse points, `read_file` is strict
  text with a `truncated` flag, `find_file` runs off the event loop, taint now covers `read_file`/`find_file`/
  `count_mail` and denies `close`/`focus`/`open_file`/`cancel_work` after it, `count_mail` reads the raw
  (unbounded) search text with page-token loop defences and is always registered, the Google account is
  injected host-side (no longer in the prompt), `close` says it closes every window of an app
  (Terminal image = `WindowsTerminal.exe`, Notepad profile added), window close does an authenticated
  `POST /shutdown` that cancels jobs before the tree is killed, a kill-on-close Job Object guards against
  launcher death, a named mutex enforces one instance, `ui_server` is retired (the app is the only host).
  Deferred: per-window close ownership (instead of per-image) and Windows-only integration tests.

## Open the app

```powershell
cd C:\Users\danie\Atlas            # after: git merge --ff-only claude/atlas-revamp
.venv\Scripts\pythonw.exe -m worker.desktop
# optional Start-menu shortcut:
powershell -ExecutionPolicy Bypass -File scripts\install_shortcut.ps1
```

Spoken acceptance round: wake → "how many unread emails do I have" → "find the <name> file" → "open it"
→ say something to the room 40 s later without "Atlas" (expect silence, `ambient` in the transcript) →
stay quiet 2 minutes (expect silent sleep) → wake → "go to sleep".
